@model DateTime?

@{ 
    var uid = Guid.NewGuid();
}

@Html.TextBox(
    string.Empty,
    Model.HasValue ? Model.Value.ToString("yyyy-MM-dd") : string.Empty,
    new { @id =uid}
    )

<script type="text/javascript">
    $("#uid").datepicker({ dateFormat: 'M d, yy' });
</script>